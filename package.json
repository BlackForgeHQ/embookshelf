{
  "name": "embookshelf",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "css:build": "tailwindcss -i web/src/styles.css -o internal/staticfs/static/app.css --minify",
    "css:watch": "tailwindcss -i web/src/styles.css -o internal/staticfs/static/app.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "tailwindcss": "^4.2.2"
  }
}
